ShowcaseScreen:
    name: 'Scatter'

    Widget:

        # Scatter:
        #     id: scatter
        size_hint: None, None
        size: image.size

        Image:
            id: image
            source: 'data/icons/bug.png'
            size: self.texture_size
            pos: 300, 200

    Button:
        size_hint: .5, .25
        pos: 20,20
        height: '48dp'
        text: 'Button normal'
        on_release: image.reload()
